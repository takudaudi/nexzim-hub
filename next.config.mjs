/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 't3.ftcdn.net', 'striped-stork-296.convex.cloud', 'upload.wikimedia.org', 'images.unsplash.com'],
    },
};

export default nextConfig;
